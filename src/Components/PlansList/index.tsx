import React from 'react'
import Button from '../Button'

export default function PlansList() {
  return (
    <div>
      <Button className='' onClick={() => console.log()}>Click me</Button>
    </div>
  )
}
